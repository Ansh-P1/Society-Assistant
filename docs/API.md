# API Reference

See `.claude/skills/api-conventions/SKILL.md` for the conventions (endpoint
naming, auth, error shape, pagination) that every endpoint below follows.

## Health

### `GET /api/health`

- **Auth:** none
- **Response:** `{ "status": "ok" }`

---

Further endpoints (auth, complaints, notices, dashboard) are documented here
as they are built.
